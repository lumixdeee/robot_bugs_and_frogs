On the creation of ban-logic for purity metaphors. AI can code hundreds of versions that do not actually fire and it can take a very long time if you have no strategy.

ROBERT:
Write a list of all the obvious things that needed to be stated for you to actually get this far
all the things humans take for granted but you need to be explicitly told
try to keep the list items human-readable

ROBOT:
Fair question. Here’s the plain, human-readable version of what had to be spelled out before the system finally behaved the way you expected:

The loop must not reuse the same input. Trying the same thing repeatedly is not retrying

A retry only counts if the candidate changes. Otherwise it is just repetition

Detection and removal must use the same rule. If they differ, things slip through or get half-processed

If something is forbidden, it must be removed or rejected everywhere. Not just at the start of a sentence or under special conditions

Punctuation does not make a word different. Quotes, brackets, or symbols should not let it bypass rules

Word families matter, not just exact matches. If the root is disallowed, all its common forms are too

You cannot rely on subtle pattern tricks for critical rules. Simple, direct matching is more reliable than clever edge handling

If a rule exists, it must be applied at only one well-labelled consistent point. Multiple scattered checks create gaps

Transforms that remove characters can hide violations. So detection must happen before or alongside transformation

A system should converge toward a valid result. Each retry should reduce the problem, not repeat it

If something fails, the system must change strategy. Otherwise it will fail the same way every time

No partial success for hard rules. Either it passes completely or it does not pass at all

All categories of forbidden content need equal treatment. Text, symbols, and semantic patterns should be handled the same way

And the biggest one:

The system must behave in a way that matches human intuition
If a person would say “that obviously still contains the forbidden thing,” the system should agree without needing extra rules

==============================

Corrections added by a horrified robot upon seeing the above list:

If the system looks like it worked but nothing actually changed, that is a failure

No visible change means the rule did not really do anything

When a rule triggers, you should be able to see what it did

If you cannot see the effect, you cannot trust that it worked

When there are multiple rules, the order they run in matters

If the order is unclear, the result will be unpredictable

Treat every input as if it might try to break things

Assuming something is safe is how problems slip through

Things that look different can still be the same underneath

Changing spacing, case, or symbols should not let something bypass the rules

Removing text can accidentally create new problems

Deleting parts can join pieces together into something forbidden

The system needs to know when to stop trying

Endless retries are just failure repeating itself

Retries should follow a clear path, not just keep trying randomly

If there is no direction or limit, the system is just wandering

You should be able to test each rule on its own

If you cannot trigger a rule directly, you cannot rely on it

Edge cases are not rare, they are where things usually break

If it only works for normal input, it is not finished

Almost blocked is the same as not blocked

Either the problem is gone, or it is still there

You should be able to compare before and after easily

If you cannot see what changed, you cannot understand what happened

Every change to the text can hide something new

Anything that reshapes input can also disguise problems

The system should make sense to a human, not just produce the right result

If the reason behind a result feels wrong, the rules need more work
